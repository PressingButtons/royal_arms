import MachineState from '../../machines/state.js';

class StampUp extends MachineState {

  constructor(machine) {
    super('stampup', machine);
  }

  exitState( ) {
  }

  update(config) {

  }

}

export default StampUp;
